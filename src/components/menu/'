.item {
  @apply flex cursor-pointer items-center;

  min-height: 30px;

  .icon{
    @apply flex-shrink-0;
  }
  .text{
    @apply flex-grow;
  }
  .arrow{
    @apply flex-shrink-0;
  }
  &:hover {
    background-color: #EDF2FF;
  }
  &.disabled {
    @apply opacity-50 cursor-not-allowed;
  }
}
